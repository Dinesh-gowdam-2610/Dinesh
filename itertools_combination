# Enter your code here. Read input from STDIN. Print output to STDOUT
import itertools
a,b = list(map(str,input().split()))
for i in range(1,int(b)+1):
    for r in itertools.combinations(sorted(a),i):
        print(''.join(r))
